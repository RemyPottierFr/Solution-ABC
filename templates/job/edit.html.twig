{% extends 'base.html.twig' %}

{% block title %}Edit Metier{% endblock %}

{% block body %}
    <h1 class="memberList__title">Modifier un métier</h1>
    <section class="container my-5">
        <div class="my-3">
            {{ include('job/_form.html.twig') }}
        </div>
        <div class="d-flex">
            <a class="btn btn-add mr-2" href="{{ path('job_index') }}">Revenir à la liste</a>
            {{ include('job/_delete_form.html.twig') }}
        </div>
    </section>
{% endblock %}
