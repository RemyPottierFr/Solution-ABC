{% extends 'base.html.twig' %}

{% block title %}Edit Prestation{% endblock %}

{% block body %}
    <h1 class="memberList__title">Modifier une prestation</h1>
    <section class="container my-5">
        <div class="my-3">
            {{ include('prestation/_form.html.twig') }}
        </div>
        <div class="d-flex">
            <a class="btn btn-add mr-2" href="{{ path('prestation_index') }}">Revenir à la liste</a>
            {{ include('prestation/_delete_form.html.twig') }}
        </div>
    </section>

{% endblock %}
